import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import BookLayout from "./pages/BookLayout.jsx";

createRoot(document.getElementById("root")).render(<BookLayout />);
